{% for post in posts %}
    <entry>
        <author>{{ post.authorName }}</author>
        <published>{{ post.createdAt|date('c') }}</published>
        <link rel="alternate">{{ _view.forum.urlForPost(post, true) }}</link>
    </entry>
{% endfor %}
