{% for topic in topics %}
    <entry>
        <title>{{ topic.subject }}</title>
        <author>{{ topic.authorName }}</author>
        <updated>{{ topic.pulledAt|date('c') }}</updated>
        <id>{{ _view.forum.urlForTopic(topic, true) }}</id>
        <link rel="alternate">{{ _view.forum.urlForTopic(topic, true) }}</link>
    </entry>
{% endfor %}
