<ul class="forum_topics_list">
{% for topic in topics %}
    <li class="topic">
        <div class="content">
            <a class="subject" href="{{ _view.forum.urlForTopic(topic) }}">{{ topic.subject }}</a>
        </div>
        <div class="metas">
            <span class="creation">Created <span class="createdAt">{{ _view.time.ago(topic.createdAt) }}</span> by
            <span class="author">{{ topic.autorName }}</span>
            | <span class="numPosts">{{ topic.numPosts }} {{ topic.numPosts > 1 ? 'posts' : 'post' }}</span>
            | <a class="category" href="{{ _view.forum.urlForCategory(topic.category) }}">{{ topic.category.name }}</a>
        </div>
    </li>
{% endfor %}
</ul>

{% include 'ForumBundle::pagination.twig' with {'pager': topics.pages, 'url': _view.forum.urlFor(category)} %}
